---
import Layout from "@layouts/Body.astro";
import siteData from "@data/site.json";

//const posts = await Astro.glob("../pages/about.md");
//const nonDraftPosts = posts.filter((post) => !post.frontmatter.draft);

// import * as about from "../pages/about-us.md";
// /const { Content } = Astro.props.post;

import { getCollection, getEntryBySlug } from "astro:content";

// Get all entries from a collection. Requires the name of the collection as an argument.
const allBlogPosts = await getCollection("about");

// Get a single entry from a collection. Requires the name of the collection and the entry's slug as arguments.
const oneBlogPost = await getEntryBySlug("about", "about-us");
---

<Layout cssClass="about">
  <div class="sitedata centered">
    {siteData.title.toUpperCase()} | {siteData.subtitle.toUpperCase()} | {
      siteData.license.name
    } - {siteData.author.name.toUpperCase()}

    <hr />
    <div>PROJECTIMAGE: {siteData.projectimage}</div>
    <hr />
    <div>author: {siteData.author.name.toUpperCase()}</div>
    <hr />
    <div>{siteData.company.name.toUpperCase()} {siteData.company.name}</div>
    <hr />
    <div>
      {siteData.company.name.toUpperCase()} MAIL: {siteData.company.email}
    </div>
    <hr />
    <div>
      {siteData.company.name.toUpperCase()} GITHUB: {siteData.company.github}
    </div>
    <hr />
    <div>
      {siteData.company.name.toUpperCase()} IG: {siteData.company.instagram}
    </div>
    <hr />
    <a href={siteData.license.url}>{siteData.license.name}</a>
    <hr />
    <a href={siteData.producer.url}>{siteData.producer.name}</a>
    <hr />
    <a href={siteData.url}>{siteData.url}</a>
    <hr />
    <div>
      {siteData.company.name.toUpperCase()} | {siteData.tldr.toUpperCase()} | v{
        siteData.version
      }
    </div>
  </div>

  <style></style>
</Layout>
