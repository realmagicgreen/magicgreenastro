---
import Logo from "@components/Logo.astro";
import FooterMenu from "@components/FooterMenu.astro";
import FooterCopy from "@components/FooterCopy.astro";

export interface Props {
  title: string;
}

const title = "fixed";
---

<footer class={title} role="contentinfo">
  <div class="container">
    <Logo />

    <FooterMenu />
  </div>

  <FooterCopy />
</footer>

<style lang="scss">
  footer {
    /* place on the bottom */
    position: sticky !important;
    background-color: var(--hearth_color);
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, 0) 0%,
      var(--hearth_color) 100%
    );
    bottom: 0;
    left: 0;
    width: 100%;
    display: grid;
    place-items: center;
    padding: 0 !important;
    padding-top: 60vh !important;
    margin-top: 100vh;
    z-index: -2;
    .container {
      padding: 50px;
    }
  }
</style>
